[project]
name = "rccar-docs"
version = "0.1.0"
description = "Wiring documentation tooling for the LEGO Technic RC car"
requires-python = ">=3.11"
dependencies = ["wireviz>=0.4,<0.5"]

[tool.uv]
package = false
